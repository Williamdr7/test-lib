import React from 'react';
import styles from './confirmationModal.scss';

export default function ConfirmationModal({
  showConfirmationModal,
  closeConfirmationModal,
}) {
  return <div></div>;
  {
    /*
    <Modal className="vulcan-bootstrap-iso" showModal={showConfirmationModal}
    showCloseButton={false}
    onClose={closeConfirmationModal}
  >
    <div id={styles['confirmation-modal-header']}>
      <p>vulcan.home.promo_card.confirmation_modal.title</p>
      <img alt="Green check mark" src={require('../../icons/greencircle_greencheck_76px.png')}/>
    </div>
    <ModalBody>
      <p className={styles['confirmation-modal-description']}>
        'vulcan.home.promo_card.confirmation_modal.description'
      </p>
      <p className={styles['confirmation-modal-footnote']}>
        vulcan.home.promo_card.confirmation_modal.footnote
      </p>
      <div className={styles['button-wrapper']}>
        <VeneerButton className="veneer-button-primary"
          id="close-confirmation-modal"
          onClick={closeConfirmationModal}
        >
          vulcan.my_account_page.cancel_enrollment.cancellation_modal.buttons.close
        </VeneerButton>
      </div>
    </ModalBody>
  </Modal>
  */
  }
}
